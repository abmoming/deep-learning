spring:
  application:
    name: eureka-server
server:
  port: 1113
eureka:
  instance:
    hostname: eurekaServerB
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eurekaA:1112/eureka